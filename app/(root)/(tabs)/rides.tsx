import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <h1>Rides</h1>
    </SafeAreaView>
  );
};

export default Rides;
