import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView>
      <h1>Chat</h1>
    </SafeAreaView>
  );
};

export default Chat;
